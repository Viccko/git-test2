# AGENTS.md

## 项目背景

这是一个高中学科知识 AI 网站，包含前端、后端、MySQL 和 DeepSeek API 接入。

## 开发规则

- 修改前先说明计划。
- 不要把 `.env` 或真实 API Key 写入代码。
- 前端不能直接请求 DeepSeek，只能请求自己的后端接口。
- 保持 `/api/chat` 请求结构稳定。
- 修改后必须运行可用的检查命令，并说明检查结果。
- 提交前必须查看 `git status` 和 `git diff`。
- MySQL 表结构以 `sql/init.sql` 为准，变更前先确认是否需要迁移说明。
- DeepSeek 调用应集中在后端服务层，避免把第三方 API 逻辑散落到路由或前端。

## 课堂优先级

优先做小步、安全、可验证的改动。
不要重构整个项目。
